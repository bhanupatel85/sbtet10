# Which StudentDataProvider the server uses. "easysbtet" (default) or "mock".
DATA_PROVIDER=easysbtet

# Server-only. Optional override for the approved Easy SBTET attendance endpoint.
EASYSBTET_API_URL=https://easysbtet.vercel.app/api/attendance
